import { IconButton } from '@mui/material'
import StopIcon from '@mui/icons-material/Stop'

const Stop = () => {
  return (
    <IconButton>
      <StopIcon />
    </IconButton>
  )
}

export default Stop
