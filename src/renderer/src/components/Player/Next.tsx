import { IconButton } from '@mui/material'
import SkipNextIcon from '@mui/icons-material/SkipNext'

const Next = () => {
  return (
    <IconButton>
      <SkipNextIcon />
    </IconButton>
  )
}

export default Next
