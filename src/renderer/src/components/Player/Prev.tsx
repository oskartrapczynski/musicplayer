import { IconButton } from '@mui/material'
import SkipPreviousIcon from '@mui/icons-material/SkipPrevious'

const Prev = () => {
  return (
    <IconButton>
      <SkipPreviousIcon />
    </IconButton>
  )
}

export default Prev
